// This is a dummy component to register the "Add" tab in the bottom tab bar.
// The actual navigation logic is intercepted in `app/(tabs)/_layout.tsx` to open the `/habit/add` screen.
export default function AddTab() {
  return null;
}
